# 일일 업무 카운터 — 설치 및 시트 연동 가이드

이 문서를 따라 하면 **나만의 업무 카운터 + 나만의 구글 스프레드시트**가 연결됩니다.
다른 팀원과 데이터가 섞이지 않으며, 회사 PC에 설치 없이 바로 쓸 수 있습니다.

전체 소요 시간: 약 5~10분 (한 번만 하면 끝)

---

## 1단계. 카운터 앱 파일 받기

전달받은 `daily-task-counter.html` 파일을 본인 PC의 원하는 폴더(예: 바탕화면, 문서 폴더)에 저장합니다.

이후로는 이 파일을 더블클릭하면 언제든 브라우저에서 카운터 앱이 열립니다. 즐겨찾기에 추가해두면 더 편합니다.

> 이 단계만으로도 카운터 앱은 바로 사용 가능합니다. 클릭 카운트와 메모는 내 컴퓨터 브라우저에 자동 저장됩니다.
> 구글 시트 연동은 "하루 데이터를 기록으로 남기고 싶을 때"만 진행하면 됩니다.

---

## 2단계. 나만의 구글 스프레드시트 만들기

1. 구글 드라이브(drive.google.com)에 접속해 로그인합니다.
2. 좌측 상단 **[+ 새로 만들기] → [Google 스프레드시트]** 를 클릭합니다.
3. 시트 이름을 알아보기 쉽게 바꿔둡니다. 예: `일일업무기록_홍길동`

---

## 3단계. 스프레드시트에 연동 코드 붙여넣기

1. 방금 만든 스프레드시트 상단 메뉴에서 **[확장 프로그램] → [Apps Script]** 를 클릭합니다.
2. 새 탭이 열리고 코드 편집기가 나타납니다. 기존에 적혀 있던 코드(`function myFunction() {}` 같은 것)를 **전체 선택해서 삭제**합니다.
3. 전달받은 `apps-script-code.gs` 파일의 내용을 **전체 복사**해서 그 자리에 붙여넣습니다.
4. 좌측 상단의 저장 아이콘(💾)을 클릭하거나 `Ctrl+S`를 누릅니다.

---

## 4단계. 웹 앱으로 배포하기

1. 화면 우측 상단의 **[배포] → [새 배포]** 버튼을 클릭합니다.
2. "유형 선택" 옆의 톱니바퀴 아이콘을 클릭하고 **[웹 앱]** 을 선택합니다.
3. 아래 항목을 입력합니다.
   - 설명: 아무 이름이나 입력 (예: `업무카운터연동`)
   - 실행 사용자: **나**
   - 액세스 권한이 있는 사용자: **모든 사용자**

   > ⚠️ 회사 구글 계정(Google Workspace)으로 만든 시트라면 "나만"으로 설정 시 카운터 앱에서 보내는 요청이 거부되는 경우가 있습니다. 반드시 **"모든 사용자"**로 설정해주세요. URL 자체가 추측 불가능한 긴 문자열이라 실질적인 보안 위험은 낮습니다.
4. **[배포]** 버튼을 클릭합니다.
5. "권한 검토가 필요합니다" 같은 안내가 나오면 **[권한 승인]** → 본인 구글 계정 선택 → **[고급]** → **[(프로젝트 이름)(안전하지 않음)으로 이동]** → **[허용]** 순서로 진행합니다.

   > 이 경고는 구글이 "내가 만든 적 없는 검증되지 않은 앱"에 대해 항상 띄우는 일반적인 보안 안내입니다. 본인이 직접 만든 개인용 도구이므로 안심하고 진행하셔도 됩니다.

6. 배포가 완료되면 **웹 앱 URL**이 화면에 나타납니다. 이 URL을 클릭해서 **전체 복사**합니다. (`https://script.google.com/macros/s/...../exec` 형태입니다.)

> 이 URL은 본인의 구글 계정 권한으로 동작하는 개인 전용 주소입니다. 외부에 공유하지 마세요.

---

## 5단계. 카운터 앱에 URL 연결하기

1. 1단계에서 저장한 `daily-task-counter.html` 파일을 더블클릭해서 엽니다.
2. 화면 상단의 **[⚙ 시트 연동 설정]** 버튼을 클릭합니다.
3. 입력창에 4단계에서 복사한 웹 앱 URL을 붙여넣습니다.
4. **[저장]** 버튼을 클릭합니다.

이제 설정이 끝났습니다.

---

## 6단계. 사용하기

하루 동안 업무를 처리할 때마다 해당 카테고리의 **[+1]** 버튼을 누릅니다. 잘못 눌렀다면 **[되돌리기]** 버튼으로 1건씩 취소할 수 있습니다.

하루 업무가 끝나면 하단의 **[📤 시트로 전송]** 버튼을 누릅니다. 이렇게 하면 그날 날짜의 4개 카운트와 메모가 본인 구글 스프레드시트에 한 줄로 저장됩니다.

전송 후에는 본인 스프레드시트를 열어 새 줄이 잘 추가됐는지 한 번 확인해보시는 것을 권장합니다 (처음 한 번만 확인하면, 이후에는 안심하고 쓰셔도 됩니다).

---

## 자주 묻는 질문

**Q. [시트로 전송]을 눌러도 시트에 아무것도 안 쌓여요.**
거의 대부분 4단계의 "액세스 권한이 있는 사용자" 설정이 원인입니다. **[배포] → [배포 관리] → 수정(연필 아이콘)** 으로 들어가서 액세스 권한이 "모든 사용자"로 되어 있는지 확인하고, 아니라면 변경 후 **새 버전**으로 다시 배포해주세요. (회사 구글 계정/Workspace 환경에서 특히 자주 발생합니다.)

**Q. 다른 팀원과 데이터가 섞이나요?**
아닙니다. 각자 본인 구글 계정으로 본인 스프레드시트를 만들고, 본인 컴퓨터의 카운터 앱에 본인 URL만 연결하기 때문에 완전히 분리되어 있습니다.

**Q. 전송 버튼을 눌러도 "성공했다"는 정확한 메시지가 안 보여요.**
구글의 보안 정책상 전송 직후 정확한 성공/실패 응답을 화면에서 받기 어렵습니다. "전송 요청을 보냈습니다" 메시지가 뜨면 시트를 열어 직접 한 번 확인해주세요.

**Q. 같은 날 전송 버튼을 두 번 누르면 어떻게 되나요?**
시트에 같은 날짜로 줄이 두 번 추가됩니다. 중복 줄이 생기면 시트에서 직접 삭제하시면 됩니다.

**Q. 회사 PC를 바꾸거나 다른 브라우저로 열면 어떻게 되나요?**
카운터 기록과 연동 설정 모두 브라우저에 저장되는 방식이라, 새 PC나 다른 브라우저에서는 처음부터 다시 5단계(URL 연결)를 해주시면 됩니다. 스프레드시트 자체는 그대로 유지되니 새로 만들지 않아도 됩니다.

**Q. 실수로 다른 사람 URL을 넣었어요.**
[⚙ 시트 연동 설정]에서 URL을 본인 것으로 다시 입력하고 저장하면 바로 교체됩니다.
